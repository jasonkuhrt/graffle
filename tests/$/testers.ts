// import { test as testBase } from 'vitest'

// export const test = testBase.extend<Fixtures>({
//   // // eslint-disable-next-line
//   // c: async ({}, use) => {
//   //   await use(context)
//   //   await context.prisma.$disconnect()
//   // },
//   // userRecord: async ({ c }, use) => {
//   //   const record = await setupUserRecord(c, { email: botEmail() })
//   //   await use(record)
//   // },
//   // user: async ({ userRecord, c }, use) => {
//   //   const token = await Janitor.Auth.forgeAccessTokenOrThrow(c, { userId: userRecord.id })
//   //   await use({
//   //     record: userRecord,
//   //     token,
//   //   })
//   // },
// })

// interface Fixtures {}
